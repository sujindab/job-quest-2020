1.  First-party cookie คือคุกกี้ที่ทำใช้เองภายในเว็บไซต์
    Third-party cookie คือคุกกี้ที่เป็นของ Third-party เช่น ดึง api facebook log-in ก็จะใช้คุกกี้จาก facebook api
2.  Consistency ข้อมูลต้องอัพเดตล่าสุดเสมอ
    Availability ข้อมูลต้องพร้อมใช้งานอยู่ตลอดเวลา
    Partition Tolerance ทำงานต่อได้ แม้จะมีโหนดใดโหนดนึงเสียไป

3.  query1 เร็วกว่า
4.  xss คือการฝังโค้ดอันตรายไว้บนหน้าเว็บที่มีช่องโหว่ เมื่อมีคนโหลดหน้าเว็บ จะทำให้โค้ดถูกทำงานด้วย
    sql injection คือการใช้ช่องโหว่ของฐานข้อมูล โดยใส่อักษรพิเศาบางอย่าง เช่น  single quote บน form ข้อมูลของหน้า UI เพื่อให้ฐานข้อมูลแสดงข้อมูลออกมาผิดปกติ

5.  callback คือ การเรียกฟังก์ชันผ่านพารามิเตอร์หรือการเรียกฟังก์ชันในฟังก์ชัน
    Promise คือ คล้ายกับ await เป็นการสัญญาว่าจะทำงานใด ๆ ให้เสร็จจนได้ผลลัพธ์
    async คือ การประกาศว่าฟังก์ชันนั้น ต้องการรอผลลัพธ์
    await ใช้งานร่วมกับ async เป็นการบอกว่าเราจะรอผลลัพธ์ของตัวแปรที่เราประกาศ await ไว้ด้านหน้า
6.  web client จะส่ง request ไปหา web server จากนั้น web server ก็จะตอบกลับ 

